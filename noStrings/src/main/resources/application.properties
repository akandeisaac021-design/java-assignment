spring.application.name=noStrings
server.port=8080

# Keep JSON error bodies simple and readable for Postman testing
server.error.include-message=always
server.error.include-binding-errors=always

# Pretty print JSON responses (nice while testing in Postman)
spring.jackson.serialization.indent_output=true
